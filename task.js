const inPutElement =document.getElementById("task");
const buttonElement =document.getElementById("add");

let inPutValue=[];

buttonElement.addEventListener("submit", (e) =>{
    e.preventDefault();
const taskDoer={
   
    
    d: newDate
}

})

localStorage.setItem("one", JSON.stringify(taskDoer))